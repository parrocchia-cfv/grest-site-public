import { RegistrationsSoonPage } from './RegistrationsSoonPage';

/** Allineato al messaggio temporaneo (link non valido / modulo assente → stessa UX di /in-arrivo). */
export function ModuleNotFound() {
  return <RegistrationsSoonPage />;
}
